# AP01 物理旋钮动作、事件与功能表

## 1. 原理示意
从按下旋钮到功能结束，到底发生了什么：
物理动作-电平信号-事件-功能映射

左右旋转、按下松开的电平信号区别：
物理动作在最底层最终都会表现为芯片输入引脚上的电平变化信号。左右旋转靠两个旋转引脚变化的先后顺序区分；按下、松开则由第三个独立引脚判断。

## 2. 原厂内置功能

### 2.1 如何确认原厂能力

通过 [`mi_cloud.py`](../../mi_cloud.py) 查询酷态科官方上传到小米云服务器的固件信息，下载与本机对应的固件，比如官方固件：ap01-1.0.2_0031.bin。
读取固件，逐步定位物理按键（物理动作）对应的电平信号、事件和功能映射证据。

#### 固件中的直接证据

| 固件中的可读记录 | 能确认的事实 |
| --- | --- |
| `Encoder CW`、`Encoder CCW` | 硬件分别识别顺时针和逆时针旋转；`CW`（顺时针方向）、`CCW`（逆时针方向） |
| `Encoder button pressed`、`Encoder button released` | 硬件分别上报按下和松开状态 |
| `key right`、`key left`、`key enter` | 原厂界面把旋钮输入转换为向右、向左和确认三类事件 |
| `j_update_screen_close`、`j_update_screen_saver` 中的 `encoder_flag` | 旋钮输入参与屏幕唤醒、熄屏和屏幕保护计时 |
| `lv_page_ui_time1_create`、`lv_page_ui_power1_create`、`lv_page_ui_weather1_create`、`lv_page_ui_date1_create`、`lv_page_ui_setting_create`、`lv_page_ui_virchar_create` | 固件中存在时钟、功率、天气、日历、设置和虚拟形象页面 |
| 各页面对应的 `key right`、`key left`、`key enter` 处理记录 | 同一个旋钮事件会根据当前页面执行不同功能 |



### 2.2 原厂 电平信号与系统事件映射表

| 物理动作 | 电平信号 | 系统事件 |
| --- | --- | --- |
| 顺时针旋转一格 | 旋转通道 A、B 按一个方向的有效次序变化；具体高低电平次序由实际接线决定 | 产生按键事件，携带 `key right` 按键码 |
| 逆时针旋转一格 | 旋转通道 A、B 按与顺时针相反的有效次序变化 | 产生按键事件，携带 `key left` 按键码 |
| 连续旋转多格 | 旋转通道 A、B 重复产生多组有效次序变化 | 连续产生携带 `key right` 或 `key left` 按键码的按键事件 |
| 按下旋钮 | 独立按钮通道从松开电平变为按下电平；具体高低极性由实际接线决定 | 产生按键事件，携带 `key enter` 按键码和按下状态 |
| 松开旋钮 | 独立按钮通道从按下电平恢复为松开电平 | 产生按键事件，携带 `key enter` 按键码和松开状态，结束本次按压周期 |
| 熄屏或屏幕保护状态下旋转或按下 | 电平信号与亮屏状态下完全相同，不存在单独的“唤醒电平” | 对应按键事件先设置 `encoder_flag`（旋钮发生操作的标记），进入统一唤醒处理 |

### 2.3 原厂系统事件与功能映射表

| 用户界面 | 物理动作 | 系统事件 | 功能映射 |
| --- | --- | --- | --- |
| 熄屏或屏幕保护 | 顺时针旋转、逆时针旋转或按下旋钮 | 携带 `key right`、`key left` 或 `key enter` 的按键事件设置 `encoder_flag` | 点亮屏幕，并重新计算屏幕保护和熄屏等待时间 |
| 一级页面 | 顺时针旋转一格 | 携带 `key right` 的按键事件 | 切换到下一个一级页面 |
| 一级页面 | 逆时针旋转一格 | 携带 `key left` 的按键事件 | 切换到上一个一级页面 |
| 一级页面 | 按下后松开 | 携带 `key enter` 的按下、松开事件 | 进入当前页面的二级内容 |
| 时钟页面 | 顺时针或逆时针旋转 | 携带 `key right` 或 `key left` 的按键事件 | 切换页面或调整当前选项 |
| 时钟页面 | 按下后松开 | 携带 `key enter` 的按下、松开事件 | 进入时间设置，确认十二小时制或二十四小时制 |
| 功率页面 | 顺时针或逆时针旋转 | 携带 `key right` 或 `key left` 的按键事件 | 选择端口、模式或设置项 |
| 功率页面 | 按下后松开 | 携带 `key enter` 的按下、松开事件 | 查看端口详情、确认模式、控制端口或相关充电功能 |
| 天气页面 | 顺时针或逆时针旋转 | 携带 `key right` 或 `key left` 的按键事件 | 浏览天气项目 |
| 天气页面 | 按下后松开 | 携带 `key enter` 的按下、松开事件 | 进入详细天气 |
| 日历页面 | 顺时针或逆时针旋转 | 携带 `key right` 或 `key left` 的按键事件 | 翻阅日期或日程 |
| 日历页面 | 按下后松开 | 携带 `key enter` 的按下、松开事件 | 进入详细日历 |
| 设置页面 | 顺时针或逆时针旋转 | 携带 `key right` 或 `key left` 的按键事件 | 选择设置项或调整数值 |
| 设置页面 | 按下后松开 | 携带 `key enter` 的按下、松开事件 | 进入并确认亮度、常亮、屏幕保护等设置 |
| 虚拟形象页面 | 顺时针或逆时针旋转 | 携带 `key right` 或 `key left` 的按键事件 | 切换到相邻的一级页面 |
| 虚拟形象页面 | 按下后松开 | 携带 `key enter` 的按下、松开事件 | 进入原厂虚拟形象选择内容 |

## 3. 用户自定义

### 3.1 可新增的动作、事件与功能表

| 动作 | 事件 | 功能 | 实现状态 |
| --- | --- | --- | --- |
| 长时间按住后松开 | 长按事件：根据按下到松开的持续时间生成 | 可设置为返回主页、返回上一级、刷新当前页面或打开快捷功能 | 硬件条件具备，等待固件实现 |
| 短时间内连续按下两次 | 双击事件：根据两次确认事件的间隔生成 | 可设置为立即刷新数据、切换常用页面或执行快捷操作 | 可通过固件新增 |
| 快速连续旋转 | 快速旋转事件：根据单位时间内的旋转格数生成 | 可设置为快速翻页、大步调整亮度或切换功能组 | 可通过固件新增 |
| 按住旋钮同时旋转 | 组合事件 | 可设置为调整亮度、切换面板类别或进入管理功能 | 需要真机验证按住期间是否继续上报旋转 |

### 3.2 当前自定义图片的旋钮行为

当前加载器把自定义图片挂载到原厂虚拟形象页面，但没有接管旋钮事件：

- 旋转旋钮仍会离开自定义图片并切换到相邻的原厂页面；
- 按下旋钮仍会进入原厂虚拟形象的二级内容；
- 自定义图片不会因为旋钮动作而切换内容；
- 要实现自定义交互，需要在设备端增加事件处理和页面状态，再把旋钮事件映射到换图、翻页或其他功能。
