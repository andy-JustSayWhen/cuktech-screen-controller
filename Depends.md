# AP01 六地址个人固件单次安装依赖

| 名称 | 作用 | 当前状态 | 阻塞性 | 维护要求 |
| --- | --- | --- | --- | --- |
| 固件工具项目远端 `opt-bin` | 提供个人固件构建、清单和连续交互模拟 | 提交 `a694f0a`，已拉取；构建程序目录干净 | 否 | 本次安装期间不切换版本或重建固件 |
| 参考项目远端 `main` | 提供小米云上传、完整回读、设备查询和单次安装流程 | 提交 `c3ac158`，安装程序已停用“仅下载”并要求上传回读后安装 | 否 | 本次操作期间不修改安装代码 |
| 参考项目 `.env` | 保存本机米家登录字段 | 已存在、权限为 `600` 且被忽略 | 否 | 不得打印、复制或提交 |
| 六地址个人固件 | 本次唯一上传与安装对象 | 长度 `6,804,520`；MD5 `045dc8a9cb3ba6510163512df90e5d69`；SHA-256 `4830f0df919f6a9df4dec1e817f76919a7474169790737785c63aaac8d3906d7`；权限 `0444` | 否 | 上传、回读和安装期间不得重建或覆盖 |
| 同名构建清单 | 证明固件完整、设备专用并允许一次测试安装 | 文件指纹一致，地址数量 6，完整性无缺项，安装许可为真 | 否 | 下发前重新读取并核对 |
| 连续交互模拟 | 证明已覆盖的页面事件规则没有失败 | 深度 8，失败数 0 | 否 | 固件变化后必须重新执行 |
| 小米云官方版本 | 证明当前基线仍适用 | 实时返回 `njcuk.enstor.ap01 / 1.0.2_0031`，官方文件指纹一致 | 否 | 正式下发前再次查询设备当前版本 |
| 第一优先级看板服务 | 安装后向 AP01 提供四页结果包 | 健康状态正常，四个数据源可用，实际结果包已完整解码 | 否 | 安装期间保持 Air 在线且地址不变 |
| 未来备用服务 | 在第一优先级不可用时按固件地址表继续尝试 | 已按用户要求预埋；部分当前未运行或位于异地网络 | 否 | 本次验收不把备用服务当作首要数据源；启用时使用同一端口和服务实现 |
| 当前账号唯一匹配 AP01 | 确定唯一目标设备并观察安装 | 已确认只有 1 台匹配设备，型号与版本匹配，在线、更新空闲、运行时间 `5,265` 秒，近 15 分钟更新错误为 0 | 否 | 上传后、正式下发前必须再次查询且条件不变 |
| 小米云文件上传 | 生成 AP01 可下载的临时地址 | 待执行 | 是 | 使用账号中真实具备上传能力的网关；临时地址不得提交 |
| 云端完整回读 | 证明云端对象与本机个人固件一致 | 待执行 | 是 | 必须完整下载并比较长度、两个文件指纹和逐字节内容 |
| 原厂恢复文件 | 保留匹配版本的未经修改固件 | 已在固件工具项目只读保存，固定基线指纹一致 | 否 | 不覆盖；只作为仍能联网时的原厂应用回刷材料 |
| 稳定供电与网络 | 保证下载、安装和重新上线 | 刷前设备在线且运行时间连续；正式下发前仍需再次确认 | 是 | 安装期间不拔电、不复位、不切换网络 |
| 用户物理验收 | 验证启动、原厂能力和目标功能 | 待安装后执行 | 是 | 自动证据通过后检查屏幕与旋钮 |
