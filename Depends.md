# 本机 AP01 图片与 Codex 额度交付依赖

| 名称 | 作用 | 类型 | 当前状态 | 阻塞性 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 项目代码与 `Goal.md` | 提供实现代码、目标范围、执行顺序和验收标准 | 软件 | 已满足 | 否 | — |
| Apple Silicon macOS 14.6.1（苹果芯片与苹果电脑系统版本） | 运行本机控制、画面生成和后台服务 | 软件 | 已满足 | 否 | — |
| Python 3.9 或更高版本（运行项目脚本的编程语言环境） | 运行登录、探测、画面生成、固件处理和服务脚本 | 软件 | 已满足 | 否 | — |
| 项目 `.venv`（与系统隔离的软件运行目录） | 隔离项目依赖，避免影响系统软件 | 软件 | 已建立并验证 | 否 | 使用 Python 3.13.0；已在隔离环境内配置可信证书文件。 |
| Pillow（读取和生成图片的程序库）、Requests（访问网络接口的程序库）及项目依赖 | 转换图片、生成面板并访问所需网络接口 | 软件 | 已安装且完整性检查通过 | 否 | — |
| RISC-V（用于这块屏幕处理器的指令体系）编译工具 | 构建仅适用于目标型号和固件的实时画面加载器 | 软件 | 待安装与验证 | 首次安装前 | 只在原厂屏幕首次安装加载器时使用。 |
| Codex 官方应用或命令行工具 | 读取本机已登录账号的额度数据 | 软件 | 已安装并登录 | 额度面板生成前复核 | 不复制账号密码或会话令牌。 |
| `mi_login.py` 小米二维码登录 | 取得小米云授权、验证目标 AP01，并保存可复用登录态 | 软件 / 账号 | 已完成并通过复用验证 | 否 | 原依赖为 macOS 米家 App；实测发现该 App 在本机“宽松安全性”环境无法启动，因此改为二维码直登。新方案实现相同授权效果，不要求改变 `SIP`（系统完整性保护）或启动安全策略，且更方便、更易用、适用范围更广。 |
| `.env` | 保存米家登录字段、AP01 唯一身份和本机工具路径等敏感配置 | 安全数据 | 已存在、已忽略、权限为 `600` | 否 | 不得提交、打印或把敏感值复制到其他文档；仓库工具自动读取。 |
| `macos/diagnose.sh` | 只读检查本机环境、后台服务、网络和设备请求日志 | 工具 | 可用 | 否 | — |
| `ap01_local_probe.py` | 只读探测 AP01 的局域网可达性 | 工具 | 已验证可用 | 否 | — |
| `ap01_prepare_screen.py` | 把普通图片或动图转换为 AP01 安全画面 | 工具 | 可用 | 否 | — |
| `ap01_screen_bridge.py` | 通过局域网提供固定自定义画面 | 工具 | 待启动 | 图片验收前 | — |
| `quota_dashboard.py` | 读取本机 Codex 额度并绘制屏幕面板 | 工具 | 待运行 | 额度验收前 | — |
| `ap01_wifi_bridge.py` | 定时刷新额度画面并提供最新屏幕文件 | 工具 | 待启动 | 额度验收前 | — |
| `macos/install-launch-agent.sh` | 设置当前用户登录后自动启动后台服务 | 工具 | 待执行 | 最终交付前 | — |
| 酷态科 10 号电能基站与 AP01 可拆卸屏幕 | 承载和显示自定义图片及额度面板 | 硬件 | 已通电、已配网、已加入米家并在线 | 否 | 执行和验证期间需要保持稳定供电，不拔电、不复位。 |
| 目标设备型号 `njcuk.enstor.ap01` | 限定实时加载器适用的唯一设备型号 | 设备身份 | 已由小米云只读验证确认 | 否 | 不得把本仓库固件偏移用于其他型号。 |
| 目标固件 `1.0.2_0031` | 限定实时加载器适用的唯一固件版本 | 设备身份 | 已由设备截图确认 | 否 | 不得把本仓库固件偏移用于其他版本。 |
| AP01 局域网地址、硬件地址和设备内部编号 | 唯一匹配本次操作的目标设备 | 设备身份 | 已保存并完成局域网与云端交叉验证 | 否 | 敏感值只保存在 `.env`。 |
| AP01 实时画面加载器 | 让原厂屏幕定时从本机读取图片；后续刷新只写入临时内存 | 设备能力 | 尚未安装，设备为完全原厂状态 | 是 | 首次安装会写入固件存储；真正安装前必须再次展示校验结果并取得用户明确确认。 |
| 匹配的 AP01 官方固件文件 | 构建并校验目标版本的实时画面加载器 | 固件材料 | 待从小米云获取并校验 | 首次安装前 | 固件文件和生成产物不得提交。 |
| FDS（小米云端临时存放固件的文件服务）上传能力或短时签名地址 | 让 AP01 在首次安装前下载并校验构建好的固件 | 云端服务 | 待检查当前账号是否有兼容网关 | 首次安装前 | AP01 自身不能申请上传地址；若当前账号没有兼容网关，需要由可信网关账号上传同一固件并提供短时地址。 |
| 同一非访客、未开启终端隔离的家庭局域网 | 让 AP01 访问本机画面服务 | 网络 | 已通过本地协议单播验证 | 否 | 不使用 USB（常见的数据线接口）或充电底座触点传输画面。 |
| 本机固定局域网地址 | 保证写入加载器的画面地址长期不变化 | 网络 | 待确认路由器已保留地址，或用户同意先按当前地址执行 | 首次安装前 | 地址变化后，原加载器将无法找到本机画面服务。 |
| TCP（让两台设备可靠传送数据的网络方式）8765 屏幕服务端口 | 允许 AP01 获取 `/screen.gif` | 网络 | 待启动服务后验证 | 图片验收前 | 防火墙、代理和安全软件不得阻断 AP01 到本机的访问。 |
| 本机互联网连接 | 刷新 Codex 额度，并在首次安装阶段访问小米云 | 网络 | 当前可用 | 执行期间 | 只显示本地图片时，加载器安装完成后不依赖互联网。 |
| 本机保持开机、当前用户保持登录且避免深度睡眠 | 保证后台服务持续生成并提供最新画面 | 运行环境 | 待用户确认长期运行方式 | 最终交付前 | — |
| 320×240、至少两帧的 GIF89a（AP01 能稳定识别的动图文件） | 作为 AP01 实际读取的图片或额度面板 | 交付产物 | 待生成 | 图片验收前 | 优先控制在 90 KB 以内；默认测试图片使用仓库自带展示图。 |
| 本机健康检查 | 证明后台服务正在运行且最新刷新状态有效 | 验收证据 | 待服务启动后取得 | 最终交付前 | 以 `http://127.0.0.1:8765/health` 的实际结果为准。 |
| AP01 `GET /screen.gif 200`（成功读取屏幕文件的网络记录） | 证明目标屏幕已经成功读取本机画面 | 验收证据 | 尚无 | 是 | 图片目标和额度目标都必须取得该证据。 |
| 两次相邻的 Codex 额度自动刷新记录 | 证明五分钟定时刷新持续工作 | 验收证据 | 尚无 | 最终交付前 | 某次读取失败时必须保留上一张成功画面。 |
| `Goal.md` 用户批准 | 授权按照目标文档开始持续执行 | 用户确认 | 待确认 | 是 | 用户需明确回复目标文档无误并允许创建目标。 |
| 首次固件安装确认 | 授权把全部校验通过的实时加载器安装到这台原厂 AP01 | 用户确认 | 尚未到确认阶段 | 是 | 该确认只能在安装前、展示完整校验结果后取得，不能提前授权。 |
