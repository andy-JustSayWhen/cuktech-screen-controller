# AP01 FW-PAGE-007-A 自动安装依赖

| 名称 | 作用 | 当前状态 | 阻塞性 | 维护要求 |
| --- | --- | --- | --- | --- |
| 参考项目远端 `main` | 提供授权、上传、完整回读与单次安装链路 | 安装状态门修复和 A 阶段回退验收已推送 | 否 | 本次操作期间不切换版本 |
| Python 3.13 | 运行参考项目脚本和测试 | 本机可用 | 否 | 正式操作前运行定向与全项目测试 |
| `.env` | 保存本机米家登录字段 | 已存在、权限为 `600` 且被忽略 | 否 | 不得打印、复制或提交 |
| 冻结成品 | 本次唯一上传与安装对象 | 只读；长度、SHA-256 与 MD5 已固定 | 否 | 操作前再次核对；期间不得重建或覆盖 |
| A 阶段成品 | 当前运行基线与在线回退对象 | 已通过开机与原厂设置物理验收 | 否 | 保持固定指纹与只读权限 |
| 当前账号唯一匹配 AP01 | 确定唯一目标设备 | 待正式操作前实时查询 | 是 | 必须是 `njcuk.enstor.ap01`、在线且当前更新空闲 |
| 小米云官方版本 | 证明当前知识与固件基线仍适用 | 待正式操作前实时查询 | 是 | 版本必须是 `1.0.2_0031`，官方 MD5 必须与已存基线一致 |
| 小米云文件上传 | 生成 AP01 可下载的临时地址 | 先前多次上传成功 | 否 | 只上传冻结成品；签名地址只保存在临时文件 |
| 云端完整回读 | 替代已禁用的仅下载命令完成安装前字节校验 | 待本次执行 | 是 | 全文件字节数和两种指纹都必须一致 |
| 安装状态序列 | 判定重启前阶段和运行时间重置 | 定向 9 项、全项目 35 项及 A 阶段真实回退通过 | 否 | 仍不得只凭进度 `101` 判定成功 |
| 稳定供电与网络 | 保证下载、安装和重新上线 | 待正式操作前由设备在线与请求成功客观复核 | 是 | 操作期间不断电、不切网、不并行下发命令 |
| 用户物理观察 | 验收安装后的启动、设置进入与文字结果 | 待自动安装完成后执行 | 是 | 本阶段不按下最后一行 |
