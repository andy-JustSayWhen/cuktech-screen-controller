# AP01 完整重写候选单次真机验证依赖

| 名称 | 作用 | 当前状态 | 阻塞性 | 维护要求 |
| --- | --- | --- | --- | --- |
| 参考项目远端 `main` | 提供二维码授权与原厂在线更新链路 | 已同步到提交 `334ae8d` | 否 | 执行期间不得切换代码版本 |
| Python 3.13 | 运行参考项目脚本 | 本机可用，所需程序库导入通过 | 否 | 运行前执行授权与安装定向测试 |
| `mi_login.py` | 生成小米二维码、验证目标 AP01、保存本机登录态 | 8 项定向测试通过，本轮扫码成功 | 否 | 登录态只写入被忽略的 `.env` |
| `.env` | 保存本机米家登录字段 | 已创建，权限为 `600` | 否 | 不得打印或提交 |
| 当前账号唯一匹配 AP01 | 确定本次唯一目标设备 | 已确认唯一、型号正确、在线且更新空闲 | 否 | 仅下载后发生预期外重启，当前运行成品身份仍待确认 |
| 云端官方版本 | 证明当前知识基线仍适用 | 已确认 `1.0.2_0031` 和官方文件指纹一致 | 否 | 正式安装前仍需保持一致 |
| 完整重写候选 | 本次仅下载与安装的唯一固件 | 本机只读文件存在，长度与 SHA-256 已复核 | 否 | 全流程必须使用同一冻结文件 |
| 构建清单 | 证明候选修改范围和离线检查 | 本机存在，安装许可保持关闭 | 是 | 只能由本次逐次授权进入真机验证，不得视为发布许可 |
| 小米临时上传能力 | 让 AP01 下载冻结成品 | 已上传并完成完整回读，逐字节一致 | 否 | 临时地址只保存到被忽略的权限 `600` 文件 |
| 稳定供电与网络 | 保证下载、安装和重新上线 | AP01 局域网可达，云端状态待查 | 是 | 执行期间不拔电、不复位、不切换网络 |
| 原厂恢复材料 | 在线异常时回到匹配原厂应用固件 | 本机固件工具项目保存匹配官方文件 | 是 | 正式安装前重新核对文件身份与恢复命令 |
| 重启后运行成品身份 | 排除仅下载已经意外切换运行内容 | 完整刷新周期内没有候选服务请求，仍待物理屏幕核对 | 是 | 服务负证据不能替代设备可见页面 |
| 即时安装确认 | 授权一次正式安装 | 因预期外重启尚未进入确认阶段 | 是 | 运行身份明确且全部门禁重新通过后再展示并确认 |
