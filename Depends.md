# AP01 启动失败回退与最小候选验证依赖

| 名称 | 作用 | 当前状态 | 阻塞性 | 维护要求 |
| --- | --- | --- | --- | --- |
| 参考项目远端 `main` | 提供二维码授权与原厂在线更新链路 | 当前分支与远端同步 | 否 | 修改仅下载入口后先提交并推送 |
| Python 3.13 | 运行参考项目脚本 | 本机可用，所需程序库导入通过 | 否 | 运行前执行授权与安装定向测试 |
| `mi_login.py` | 生成小米二维码、验证目标 AP01、保存本机登录态 | 8 项定向测试通过，本轮扫码成功 | 否 | 登录态只写入被忽略的 `.env` |
| `.env` | 保存本机米家登录字段 | 已创建，权限为 `600` | 否 | 不得打印或提交 |
| 当前账号唯一匹配 AP01 | 确定本次唯一目标设备 | 已确认唯一、型号正确、在线且更新空闲 | 否 | 安装前重新查询 |
| 云端官方版本 | 证明当前知识基线仍适用 | 已确认 `1.0.2_0031` 和官方文件指纹一致 | 否 | 正式安装前仍需保持一致 |
| 启动失败候选 | 失败证据与禁止复用对象 | 已确认卡在开机动画，永久禁用 | 否 | 不得再次上传、下载或安装 |
| 最小候选构建清单 | 证明低栈协议、修改范围和离线检查 | 待固件工具项目生成 | 是 | 下载状态不超过 160 字节，单函数静态栈不超过 320 字节 |
| 小米临时上传能力 | 让 AP01 下载冻结成品 | 恢复成品已完成完整回读；新候选待上传 | 否 | 临时地址只保存到被忽略的权限 `600` 文件 |
| 稳定供电与网络 | 保证下载、安装和重新上线 | AP01 局域网可达，云端状态待查 | 是 | 执行期间不拔电、不复位、不切换网络 |
| 原厂恢复材料 | 在线异常时回到匹配原厂应用固件 | 本机固件工具项目保存匹配官方文件 | 是 | 正式安装前重新核对文件身份与恢复命令 |
| 已验收 `opt-setting.bin` | 从启动失败候选恢复到已验收设置阶段 | 只读副本和云端完整回读逐字节一致，已下发一次回退 | 否 | 固定 SHA-256 为 `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` |
| 小米云更新通道 | 在线回刷已验收设置阶段成品 | 已完成重启和重新上线，状态为空闲 | 否 | 未重复下发 |
| 回退后物理画面 | 确认设备已经离开开机动画并进入一级页面 | 等待用户最终确认；自动在线证据稳定 | 否 | 新候选安装前仍需纳入即时风险说明 |
| 重启后运行成品身份 | 排除仅下载已经意外切换运行内容 | 用户确认卡开机动画，候选启动失败 | 否 | 当前候选禁止再次安装 |
| 即时安装确认 | 授权一次正式安装 | 最小候选冻结后申请 | 是 | 必须绑定候选文件名、长度与指纹 |
