# AP01 FW-PAGE-007-A 失败回刷依赖

| 名称 | 作用 | 当前状态 | 阻塞性 | 维护要求 |
| --- | --- | --- | --- | --- |
| 参考项目远端 `main` | 提供授权、上传、完整回读与单次回刷链路 | 安装状态门与上次 A 阶段回退已验证 | 否 | 本次操作期间不切换版本 |
| `.env` | 保存本机米家登录字段 | 已存在、权限为 `600` 且被忽略 | 否 | 不得打印、复制或提交 |
| A 阶段冻结成品 | 本次唯一上传与回刷对象 | 已通过启动与原厂设置物理验收；固定指纹见 `Goal.md` | 否 | 操作前再次核对；期间不得重建或覆盖 |
| 当前账号唯一匹配 AP01 | 确定唯一目标设备 | 断电重启后唯一、在线、更新空闲、运行时间 49 秒；近 15 分钟的 1 条错误来自断电前任务 | 否 | 新任务下发前再查询一次；不得操作其他设备 |
| 小米云官方版本 | 证明当前基线仍适用 | 上次安装前为 `1.0.2_0031`，官方 MD5 与已存基线一致 | 否 | 回刷前再次实时查询 |
| 官方回退文件 | 保留未修改的匹配版本 | 已保存，指纹与小米云一致且只读 | 否 | 不覆盖，作为 A 阶段回刷失败时的后备在线回退对象 |
| 小米云文件上传 | 生成 AP01 可下载的临时地址 | 直连登录请求曾在上传前中断；复用当前 `7897` 代理后上传成功 | 否 | 不切换配置；签名地址只存在权限 `600` 的临时文件 |
| 云端完整回读 | 完成回刷前字节校验 | 回读 `6,804,520` 字节，两种指纹和逐字节比较全部一致 | 否 | 回刷必须使用当前已校验的同一文件 |
| 安装状态序列 | 判定重启前阶段和运行时间重置 | 断电重启已清除未知下载状态；新任务尚未下发 | 否 | 新任务仍只下发一次，保留完整状态序列 |
| 稳定供电与网络 | 保证下载、回刷和重新上线 | 设备云端在线且更新空闲 | 否 | 回刷期间不断电、不切网、不并行下发命令 |
| 用户物理操作 | 解除未知下载状态并提供新的启动现场 | 已断电重启，运行时间 49 秒 | 否 | 自动回刷完成后再交由用户确认一级页面 |
