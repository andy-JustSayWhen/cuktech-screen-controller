# AP01 完整重写候选单次真机验证依赖

| 名称 | 作用 | 当前状态 | 阻塞性 | 维护要求 |
| --- | --- | --- | --- | --- |
| 参考项目远端 `main` | 提供二维码授权与原厂在线更新链路 | 已同步到提交 `334ae8d` | 否 | 执行期间不得切换代码版本 |
| Python 3.13 | 运行参考项目脚本 | 本机可用，所需程序库导入通过 | 否 | 运行前执行授权与安装定向测试 |
| `mi_login.py` | 生成小米二维码、验证目标 AP01、保存本机登录态 | 代码与测试已存在，等待本机扫码 | 是 | 登录态只写入被忽略的 `.env` |
| `.env` | 保存本机米家登录字段 | 当前不存在 | 是 | 扫码成功后权限必须为 `600`，不得打印或提交 |
| 当前账号唯一匹配 AP01 | 确定本次唯一目标设备 | 等待扫码后重新查询 | 是 | 必须同时确认型号、版本、在线与更新空闲 |
| 云端官方版本 | 证明当前知识基线仍适用 | 等待扫码后重新查询 | 是 | 必须仍为 `1.0.2_0031` 且官方文件指纹一致 |
| 完整重写候选 | 本次仅下载与安装的唯一固件 | 本机只读文件存在，长度与 SHA-256 已复核 | 否 | 全流程必须使用同一冻结文件 |
| 构建清单 | 证明候选修改范围和离线检查 | 本机存在，安装许可保持关闭 | 是 | 只能由本次逐次授权进入真机验证，不得视为发布许可 |
| 小米临时上传能力 | 让 AP01 下载冻结成品 | 等待扫码后核对兼容网关 | 是 | 上传后必须完整回读并逐字节比较 |
| 稳定供电与网络 | 保证下载、安装和重新上线 | AP01 局域网可达，云端状态待查 | 是 | 执行期间不拔电、不复位、不切换网络 |
| 原厂恢复材料 | 在线异常时回到匹配原厂应用固件 | 本机固件工具项目保存匹配官方文件 | 是 | 正式安装前重新核对文件身份与恢复命令 |
| 即时安装确认 | 授权一次正式安装 | 尚未取得确认门所需的完整展示后确认 | 是 | 仅下载成功后再向用户展示并取得确认 |
