# AP01 六地址个人固件单次安装目标

## 当前目标

1. 只安装已经在固件工具项目生成并设为只读的六地址个人固件，不重新构建或覆盖。
2. 安装前确认常开 MacBook Air 的看板服务健康，四页结果包可以完整解码。
3. 通过既有小米云文件上传流程上传同一份固件，从签名地址完整回读并逐字节比较。
4. 刷前连续交互模拟通过，且设备身份、版本、在线状态、更新空闲状态、固件文件和回读结果没有变化时，只下发一次正式安装，不再次询问。
5. 观察设备接受任务、到达重启前阶段、运行时间重置、重新上线、更新状态恢复空闲且本次没有新增更新错误。
6. 自动检查通过后，由用户肉眼验收原厂页面、米家通信、官方更新能力和四页 AGENTS 看板。

## 当前证据

- 固件工具项目分支为 `opt-bin`，提交为 `a694f0a`；构建程序目录没有本地改动。
- 小米云实时返回型号 `njcuk.enstor.ap01`、版本 `1.0.2_0031`；官方原版文件长度和两个文件指纹与固定基线一致。
- 本次个人固件路径为 `/Users/mac/Library/Application Support/Cuktech/AP01/firmware/ap01-1.0.2_0031-opt-personalized.bin`。
- 个人固件长度为 `6,804,520` 字节，MD5 为 `045dc8a9cb3ba6510163512df90e5d69`，SHA-256 为 `4830f0df919f6a9df4dec1e817f76919a7474169790737785c63aaac8d3906d7`，权限为 `0444`。
- 构建清单已确认：状态允许一次测试安装、设备专用、取数程序启用、地址数量为 6、完整性无缺项、安装许可为真。
- 深度 8 连续页面事件模拟通过，失败数为 0。
- 第一优先级 MacBook Air 看板服务健康，四个数据源可用；实际结果包返回成功并完整解码为四页。
- 其余地址是按用户要求预埋的异地或未来备用服务；本次安装后的首要取数验收只使用第一优先级服务。
- 刷前只读查询确认：当前账号只有 1 台匹配设备，型号与版本匹配，设备在线，更新状态为空闲，运行时间为 `5,265` 秒，近 15 分钟更新错误为 0；进度字段为历史值 `101`。

## 范围

- 只处理当前账号中唯一匹配的 `njcuk.enstor.ap01` 专用测试设备。
- 只处理官方基线版本 `1.0.2_0031`。
- 只使用上述只读个人固件和同名构建清单，不制作其他固件。
- 上传、回读和安装必须使用逐字节相同的文件。
- 不切换网络配置，不操作其他设备，不并行下发其他更新任务。

## 执行顺序

1. [x] 拉取固件工具项目最新内容并读取 `SOP/如何刷机.md`。
2. [x] 查询小米云官方版本，判定公开优化固件类型并确认必须个人化。
3. [x] 收集六个服务地址，在已支持的 macOS 环境生成个人固件。
4. [x] 完成构建清单、文件指纹、完整性、安装许可和连续交互模拟检查。
5. [x] 检查第一优先级服务健康并完整解码四页结果包。
6. [x] 只读查询唯一目标设备的型号、版本、在线状态、运行时间、更新状态和近期更新错误。
7. [ ] 上传同一份个人固件，从签名地址完整回读并逐字节比较。
8. [ ] 再次执行下发前现场检查；全部条件未变化时只下发一次正式安装。
9. [ ] 观察重启前阶段、运行时间重置、重新上线、更新空闲和本次更新错误。
10. [ ] 删除临时签名地址、回读副本和运行日志。
11. [ ] 由用户完成屏幕、旋钮、原厂页面、米家通信、官方更新能力和目标功能验收。

## 验收标准

- 自动证据：上传回读与本机只读个人固件逐字节一致；安装只下发一次；设备到达重启前阶段，运行时间重置，重新上线，更新状态恢复空闲且本次没有新增更新错误。
- 服务证据：安装后第一优先级服务出现设备真实请求，返回的数据包通过完整解码。
- 物理验收：用户确认设备离开开机动画，原厂页面与旋钮正常，AGENTS 看板能显示、切换并返回。

## 安全停止条件

- 目标设备不唯一、型号或版本不匹配、设备离线、更新状态非空闲或近期出现无法解释的更新错误。
- 个人固件文件名、长度、权限、两个文件指纹、清单状态或模拟结果任一变化。
- 第一优先级看板服务不健康，或实际结果包无法完整解码。
- 上传对象无法完整回读，或回读副本与本机固件不一致。
- 供电、网络或设备身份在下发前发生变化。
- 下发后状态无法判断、运行时间没有重置或设备没有重新上线；不得重复盲目下发。
- 任一登录字段、设备内部编号、临时签名地址、个人固件或临时日志进入待提交内容。
