# AP01 FW-PAGE-007-A 失败回刷目标

## 当前目标

1. 禁止再次安装卡开机动画的 `FW-PAGE-007-A`。
2. 只使用已通过启动和原厂设置物理验收的 A 阶段冻结成品执行在线回刷；遇到未知状态立即停止。
3. 上传对象必须完整回读并与 A 阶段成品逐字节一致。
4. 回刷只下发一次，确认到达重启前阶段、运行时间重置、重新上线、更新空闲且没有本次更新错误。
5. 回刷完成后只交由用户确认已进入正常一级页面，不做长时间观察。

## 当前状态

用户确认 `FW-PAGE-007-A` 卡开机动画。失败后实时查询只找到一台目标型号设备，设备云端在线、
更新空闲、运行时间为 1,001 秒，近 10 分钟更新错误为 0。这些结果只证明在线回刷通道可用。

A 阶段回刷成品固定为
`ap01-1.0.2_0031-page-settings-startup-passthrough.bin`，长度 `6,804,520` 字节，SHA-256 为
`8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b`，MD5 为
`a066fab43085e8ab4c8b8aa33ff8af6f`，权限为 `0444`。

首次上传尝试在小米账号登录请求阶段遇到加密连接中断，失败发生在上传与设备任务之前。
没有切换网络配置，复用当前 `7897` 代理重试成功。A 阶段成品已完整回读 `6,804,520` 字节，
回读副本的两种指纹和逐字节比较全部一致。

任务只下发一次。0 秒读到下载中、进度 0；随后状态与运行时间不可读，211 秒仍只能读到
进度 0，360 秒内没有观察到重启前阶段或运行时间重置。结束后单次查询显示设备云端在线、
状态仍为下载中、进度和运行时间不可读，近 15 分钟新增 1 条更新错误。本次没有成功证据，
已停止且没有重复下发。临时签名地址、回读副本和运行日志已删除。

用户已按要求断电重启。重启后只读查询仍只找到一台目标设备：设备在线、更新空闲、
进度为 101、运行时间为 49 秒。近 15 分钟的 1 条更新错误属于断电前唯一一次失败任务；
断电后尚未下发新任务。未知下载状态已经清除，允许重新生成临时地址、完整回读，再执行
一次 A 阶段回刷；任何身份、回读或现场检查不一致都立即停止。

同一 A 阶段成品已重新上传并完整回读。回读长度为 `6,804,520` 字节，SHA-256、MD5
和逐字节比较均与本机只读成品一致；回读速度为每秒 `2,534,899` 字节。签名地址、回读
副本和上传日志只保存在权限 `600` 的临时目录中。下一步只剩下发前现场复核和单次回刷。

第一次下发前检查读到设备唯一、在线、更新空闲、运行时间 306 秒，近 15 分钟更新错误为 0。
临时检查器仍要求错误数等于已经超出时间窗口的旧值 1，因此在设备任务之前主动停止。
本次没有下发。检查条件改为近期错误必须为 0 后，重新执行下发前检查和单次回刷。

纠正后的检查读到设备唯一、在线、更新空闲、运行时间 387 秒，近 15 分钟更新错误为 0。
回刷只下发一次：124 秒达到已下载、进度 100；128 秒达到已安装；131 秒运行时间从
519 秒重置为 7 秒并重新上线。即时收尾查询显示设备唯一、在线、更新空闲、运行时间
27 秒，近 10 分钟本次更新错误为 0。临时签名地址、回读副本和全部运行日志已删除。

## 范围

- 只处理当前账号中唯一匹配的 `njcuk.enstor.ap01` 专用测试设备。
- 只处理官方版本 `1.0.2_0031`。
- 只使用既有只读 A 阶段成品，不重新构建、不覆盖。
- 不切换网络配置，不操作其他设备，不并行下发其他命令。

## 执行顺序

1. [x] 更新固件工具项目的失败证据、DESIGN 和门禁，提交并推送远端。
2. [x] 更新本文与 `Depends.md`，提交并推送参考项目远端 `main`。
3. [x] 核对 A 阶段冻结成品身份与当前设备现场。
4. [x] 上传 A 阶段成品，从云端签名地址完整回读并逐字节比较。
5. [x] 再次查询设备现场，只下发一次正式回刷；任务未出现成功证据，按停止条件终止。
6. [x] 查询重启前阶段、运行时间、在线状态和更新错误；确认不能判定回刷完成。
7. [x] 删除临时签名地址、回读副本和运行日志。
8. [x] 用户断电重启一次；设备重新上线后查询为唯一、在线、更新空闲、运行时间 49 秒。
9. [x] 重新上传同一 A 阶段成品并完整回读；长度、两种指纹和逐字节比较全部一致。
10. [x] 第一次下发前检查现场安全，但旧错误计数条件过时；检查器已在设备任务前停止。
11. [x] 按近期错误必须为 0 的条件重新检查，只下发一次回刷。
12. [x] 确认已下载、已安装、运行时间重置、重新上线、更新空闲且本次错误为 0；临时材料已删除。

## 验收标准

- 自动证据：上传回读与 A 阶段成品逐字节一致；回刷只下发一次；设备已完成安装、重启并
  重新上线；即时查询为更新空闲且本次错误为 0。已通过。
- 物理验收：用户确认已进入正常一级页面。

## 安全停止条件

- 官方版本、目标设备、A 阶段文件名、长度、权限或指纹任一不匹配。
- 设备不唯一、离线、更新状态非空闲，或出现不能归属于断电前失败任务的近期更新错误。
- 上传对象无法完整回读，或回读副本与 A 阶段成品不一致。
- 下发后进度不前进、设备状态无法判断、运行时间没有重置，或设备没有重新上线；不得重复盲目下发。
- 任凭证、设备标识、签名地址、固件文件或临时日志进入待提交内容。
