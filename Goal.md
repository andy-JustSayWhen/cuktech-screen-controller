# AP01 双调用者候选失败回退目标

## 当前目标

1. 永久禁用进入设置后死机的 `FW-PAGE-006-A`，不得重复下发。
2. 复用已经通过启动验收的 A 阶段固定成品执行一次在线回刷。
3. 上传对象必须完整回读并与 A 阶段成品逐字节一致。
4. 只下发一次回刷，确认设备重启、重新上线、更新空闲且没有本次更新错误。
5. 回退完成后立即交由用户确认原厂设置可以正常进入。

## 当前状态

`FW-PAGE-006-A` 启动进入一级页面通过，但用户在一级设置页按下旋钮后设备立即死机。固件工具
项目已经把该成品标为否定并禁止再次安装；三种扩展原厂设置列表的结构全部停止。

A 阶段成品已由用户确认可以启动，固定文件为
`ap01-1.0.2_0031-page-settings-startup-passthrough.bin`，只允许使用既有只读成品及固定指纹回退。

首次回退已完成上传与云端完整回读并只下发一次，但设备只返回下载中、进度 0，随后更新状态、
进度和运行时间均不可读。当前没有下载或重启证据，未重复下发；必须先由用户断电重启一次。

用户已经完成断电重启。设备重新在线、更新空闲且运行时间重新计数；重启前旧任务的错误记录不
代表当前任务。旧上传地址已经删除，当前重新上传 A 阶段成品并执行全量回读。

## 范围

- 只处理当前账号唯一匹配的 `njcuk.enstor.ap01` 专用测试设备。
- 只处理版本 `1.0.2_0031`。
- 本次不修改安装工具、授权、设备选择或固件制作逻辑，只执行回退。
- `FW-PAGE-004-B`、`FW-PAGE-005-A` 与 `FW-PAGE-006-A` 禁止再次上传或安装。
- 回退对象固定为固件工具项目的
  `ap01-1.0.2_0031-page-settings-startup-passthrough.bin`。
- 不切换网络配置，不操作其他设备。

## 执行顺序

1. 先更新本文件与 `Depends.md`，提交并推送参考项目远端 `main`。
2. 重新查询官方版本、唯一设备、在线状态、更新空闲和错误记录。
3. 重新上传 A 阶段固定成品并完整回读，逐字节一致后只下发一次回刷。
4. 确认重启、重新上线、更新空闲且没有本次更新错误后，立即提示用户进入原厂设置。

## 验收标准

- 上传对象与 A 阶段固定成品逐字节一致。
- 回刷只下发一次，设备重启、运行时间重新计数并重新上线。
- 用户确认一级页面与原厂设置恢复正常；自动检查不替代物理验收。

## 安全停止条件

- 官方版本、A 阶段成品指纹或目标设备任一不一致。
- 设备不唯一、离线、更新状态非空闲，或出现更新错误。
- 上传对象与冻结成品无法证明逐字节一致。
- 下载没有进展、运行时间没有重置或设备没有重新上线；不得重复盲目下发。
- 凭据、设备标识、固件文件或临时下载地址进入待提交内容。
