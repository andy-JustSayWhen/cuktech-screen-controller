# AP01 首尾立即定位修正版单次安装目标

## 当前目标

1. 使用固件工具项目第 21 节批准范围生成首尾立即定位修正版 `opt-setting.bin`。
2. 上传冻结成品并完整回读，逐字节确认与本机文件一致。
3. 在当前账号唯一匹配 AP01 上执行一次原厂普通在线更新安装。
4. 验证设备接受、下载、安装、重启、重新上线和短时稳定性。

## 范围

- 只处理型号 `njcuk.enstor.ap01`、版本 `1.0.2_0031`。
- 只处理第 21 节批准范围生成的设置菜单阶段成品。
- 继续由 AP01 原厂更新程序决定写入位置。
- 不修改参考项目程序、登录态、画面服务、网络配置或其他设备。

## 执行顺序

1. 记录批准并重新校验官方版本、原厂基线和唯一目标设备。
2. 生成批准范围绑定清单和只读冻结成品。
3. 完整验证成品修改范围、文件尾校验、测试和文件指纹。
4. 上传后完整回读并逐字节比较。
5. 下发一次安装并持续检查重启、在线状态和运行时间。

## 验收标准

- 构建成品只包含批准区间和文件尾恢复校验变化。
- 上传对象与冻结成品逐字节一致。
- 设备完成安装、重启、重新上线，运行时间连续增长且更新状态为空闲。
- 用户能够在一级设置菜单真机验证两个首尾方向均立即显示目标项。

## 安全停止条件

- 官方版本、基线文件、批准范围或目标设备任一不一致。
- 设备不唯一、离线、更新状态非空闲，或供电、网络不稳定。
- 上传对象与冻结成品无法证明逐字节一致。
- 下载、安装、重启或重新上线出现异常；不重复盲目下发。
- 凭据、设备标识、固件文件或临时下载地址进入待提交内容。
