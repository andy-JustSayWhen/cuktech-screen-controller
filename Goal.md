# AP01 安装状态门纠偏与双调用者候选安装目标

## 当前目标

1. 修正 AP01 在“已下载、进度 50”后直接重启时被安装工具误报失败的问题。
2. 保持安装命令只下发一次；状态误报不得触发重复安装。
3. 为该真实状态序列增加自动测试并推送参考项目远端 `main`。
4. 复用固件工具项目已经冻结的唯一 `FW-PAGE-006-A` 成品，完成上传、完整回读、单次安装和
   重启重新上线验证。
5. 自动证据完成后立即交由用户分别确认启动和进入设置两个物理结果。

## 当前状态

`FW-PAGE-005-A` 已由用户确认卡在开机动画并永久禁用。已验收 A 阶段完成同一次在线回刷、
重启和重新上线；设备在线、更新空闲且没有本次更新错误。

固件工具项目已经完成 `FW-PAGE-006-A` 的双调用者设计、114 项全项目测试、两次确定性构建和
冻结指纹。该候选保持开机调用原厂七项，只在用户确认进入设置时的第二次原厂调用追加一项。

当前安装工具在设备先上报“已下载、进度 50”，随后运行时间重置并重新上线时，仍因没有观察到
“安装中”“已安装”或进度 100 而误报失败。该状态门必须先纠偏，随后才能安装新候选。

## 范围

- 只处理当前账号唯一匹配的 `njcuk.enstor.ap01` 专用测试设备。
- 只处理版本 `1.0.2_0031`。
- 只修改安装完成状态识别和对应测试，不改变授权、上传、设备选择或固件制作逻辑。
- `FW-PAGE-004-B` 与 `FW-PAGE-005-A` 禁止再次上传或安装。
- 新候选固定为固件工具项目的
  `ap01-1.0.2_0031-page-settings-delayed-row-creation.bin`。
- 不切换网络配置，不操作其他设备。

## 执行顺序

1. 先更新本文件、`Depends.md` 和固件安全规则中的完成状态判据。
2. 把“已下载”纳入已观察到安装前阶段；仍必须同时观察运行时间重置，不能仅凭进度 101 成功。
3. 增加“已下载 50 → 空闲 101 → 运行时间重置”的自动测试，运行定向与全项目测试。
4. 提交并推送参考项目远端 `main`。
5. 重新查询官方版本、唯一设备、在线状态、更新空闲和错误记录。
6. 上传冻结成品并完整回读，逐字节一致后只下发一次安装。
7. 确认重启、重新上线、更新空闲且没有本次更新错误后，立即提示用户查看屏幕。

## 验收标准

- 自动测试证明“已下载”本身不等于安装完成；只有随后运行时间重置才返回成功。
- 进度 101 且没有已下载、安装中、已安装或进度 100 证据时仍判失败。
- 上传对象与冻结成品逐字节一致。
- 正式安装只下发一次，设备重启、运行时间重新计数并重新上线。
- 用户分别确认能进入一级页面和进入设置后显示八项；自动检查不替代物理验收。

## 安全停止条件

- 官方版本、冻结成品、构建清单或目标设备任一不一致。
- 设备不唯一、离线、更新状态非空闲，或出现更新错误。
- 上传对象与冻结成品无法证明逐字节一致。
- 下载没有进展、运行时间没有重置或设备没有重新上线；不得重复盲目下发。
- 凭据、设备标识、固件文件或临时下载地址进入待提交内容。
