# AP01 FW-PAGE-007-A 自动安装目标

## 当前目标

1. 只安装固定成品 `ap01-1.0.2_0031-page-settings-return-label.bin`。
2. 上传对象必须完整回读并与冻结成品逐字节一致。
3. 现场设备、官方版本、更新状态与错误记录都通过后，只下发一次正式安装。
4. 确认设备到达重启前阶段、运行时间重置、重新上线、更新空闲且没有本次更新错误。
5. 安装后交由用户验收启动、原厂设置进入和最后一行文字；本阶段不按下最后一行。

## 当前状态

`FW-PAGE-007-A` 已通过定向 4 项、全项目 118 项测试和两次隔离完整构建。
成品长度为 `6,804,520` 字节，SHA-256 为
`00e5fab6bab3fcc5b3aa4b9716c858e26955759c3a89c06b04feb6184c7d2df3`，MD5 为
`b562917d96dcdde627153e9d6d03a5aa`，权限为 `0444`。

固件工具项目的制作代码与构建证据已推送到远端 `opt-bin`。当前设备运行 A 阶段
回退基线，用户已确认一级页面和原厂设置正常。

用户已明确规定“模拟通过后直接刷机，不得再询问”。本次在当前任务中按该门禁自动推进，
不使用已被真机否定的仅下载命令，以上传后云端完整回读作为安装前字节验证。

参考项目安装定向测试 6 项和全项目 35 项已通过。实时查询只找到一台目标型号设备，
设备在线，更新状态空闲，近 30 分钟更新错误为 0，运行时间为 2,431 秒。云端官方版本为
`1.0.2_0031`，MD5 为 `469a7329d496f81ae8625c4d76ccf56d`。与该指纹一致的官方回退文件已保存且只读。

冻结成品已上传，从临时签名地址完整回读 `6,804,520` 字节。回读副本与冻结成品的
SHA-256 和 MD5 均一致，逐字节比较通过。临时签名地址和回读副本只保留到本次安装结束。

## 范围

- 只处理当前账号中唯一匹配的 `njcuk.enstor.ap01` 专用测试设备。
- 只处理官方版本 `1.0.2_0031`。
- 只使用固件工具项目中已冻结的只读成品，不重新构建、不原地覆盖。
- 不切换网络配置，不操作其他设备，不并行下发其他命令。
- A 阶段成品保留为在线回退对象；已否定的候选不得重复上传或安装。

## 执行顺序

1. [x] 更新本文与 `Depends.md`，提交并推送参考项目远端 `main`。
2. [x] 运行定向与全项目安装测试，核对冻结成品身份。
3. [x] 查询官方最新版本、唯一目标设备、在线状态、当前更新状态、运行时间和近期错误。
4. [x] 上传冻结成品，从云端签名地址完整回读，并逐字节比较。
5. [ ] 重新查询设备现场，只下发一次正式安装。
6. [ ] 记录重启前阶段、运行时间重置、重新上线、更新空闲和错误查询结果。
7. [ ] 删除临时签名地址、回读副本和运行日志，交接物理验收。

## 验收标准

- 自动证据：上传回读与冻结成品逐字节一致；安装只下发一次；设备完成重启前阶段、
  运行时间重置、重新上线并回到更新空闲；没有本次更新错误。
- 物理验收：正常进入一级页面；设置页可以正常进入；列表仍为七个对象；最后一行显示
  “开关一级页面”；不按下最后一行。

## 安全停止条件

- 官方版本、目标设备、冻结文件名、字节数、权限或指纹任一不匹配。
- 设备不唯一、离线、更新状态非空闲，或出现未解释的近期更新错误。
- 上传对象无法完整回读，或回读副本与冻结成品不一致。
- 下发后进度不前进、设备状态无法判断、运行时间没有重置，或设备没有重新上线；不得重复盲目下发。
- 任凭证、设备标识、签名地址、固件文件或临时日志进入待提交内容。
