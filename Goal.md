# AP01 启动失败回退与最小候选验证目标

## 当前目标

1. 保持启动失败的完整重写候选永久禁用。
2. 复核 `opt-setting.bin` 在线回退后的设备状态与更新日志。
3. 从同一已验收输入构建只含低栈四页传输和原厂萌宠三个局部分支的最小候选。
4. 上传最小候选并完整回读，逐字节确认与本机文件一致。
5. 展示目标设备、确切文件指纹、失败风险和恢复材料，取得即时安装确认。
6. 确认后只下发一次正式安装，并验证接受、下载、安装、重启、重新上线和短时稳定性。

## 当前状态

用户物理确认 AP01 卡在开机动画；标为仅下载的动作实际触发完整重写候选运行，候选启动失败。
已验收 `opt-setting.bin` 完成只读副本、云端完整回读和一次在线回退；设备已经重新上线、
运行计时连续增长且更新状态为空闲，没有新增更新错误。自动证据支持回退后程序稳定运行；
物理画面恢复仍由用户最终确认。

## 范围

- 只处理当前账号唯一匹配的 `njcuk.enstor.ap01` 专用测试设备。
- 只处理版本 `1.0.2_0031`。
- 启动失败候选禁止再次上传、下载或安装。
- 新候选必须从 SHA-256 为
  `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1`
  的 `opt-setting.bin` 只读副本构建。
- 继续由 AP01 原厂在线更新程序决定写入位置。
- 允许修改参考项目，永久关闭已经被实测否定的仅下载入口。
- 不切换网络配置，不操作其他设备。

## 执行顺序

1. 只读查询唯一目标设备、在线状态、运行计时和更新错误。
2. 按固件工具项目 DESIGN 先改文档，再实现低栈协议和最小组合。
3. 完成全部离线门禁并冻结唯一候选。
4. 上传冻结成品，完整下载回本机并逐字节比较；不再执行仅下载。
5. 展示即时安装确认材料，等待用户明确确认。
6. 下发一次安装，持续检查设备状态、重启、上线和更新错误。
7. 自动检查通过后，由用户执行全部页面与物理旋钮验收。

## 验收标准

- 上传对象与冻结成品逐字节一致。
- 下载状态结构、单函数静态栈和修改区间满足 DESIGN 门禁。
- 正式安装后设备完成重启并重新上线，运行时间重新计数。
- 米家通信、原厂基础页面和官方更新能力保持正常。
- 用户完成完整页面与物理旋钮验收；自动检查不得替代物理验收。

## 安全停止条件

- 官方版本、冻结成品、构建清单或目标设备任一不一致。
- 设备不唯一、离线、更新状态非空闲，或供电、网络不稳定。
- 上传对象与冻结成品无法证明逐字节一致。
- 出现任何更新错误。
- 未取得正式安装前的即时确认。
- 下载、安装、重启或重新上线出现异常；不重复盲目下发。
- 凭据、设备标识、固件文件或临时下载地址进入待提交内容。
