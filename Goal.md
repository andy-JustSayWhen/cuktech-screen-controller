# AP01 完整重写候选单次真机验证目标

## 当前目标

1. 使用 `mi_login.py` 完成小米二维码授权，并确认账号中只有一台匹配的 AP01。
2. 复核目标设备型号、当前版本、在线状态、更新状态和云端官方版本。
3. 复核固件工具项目冻结的 `ap01-1.0.2_0031-opt-rewrite-candidate.bin`。
4. 上传同一冻结成品并完整回读，逐字节确认与本机文件一致。
5. 先执行仅下载校验；成功后展示本次设备、成品、风险和恢复材料，取得即时安装确认。
6. 确认后只下发一次正式安装，并验证接受、下载、安装、重启、重新上线和短时稳定性。

## 当前状态

用户物理确认 AP01 卡在开机动画；仅下载动作实际触发完整重写候选运行，候选启动失败。
设备仍能通过小米云访问且更新状态为空闲。当前目标切换为：禁止再次下发该候选，核对并
回刷已验收 `opt-setting.bin`，恢复设备进入一级页面。

## 范围

- 只处理当前账号唯一匹配的 `njcuk.enstor.ap01` 专用测试设备。
- 只处理版本 `1.0.2_0031`。
- 只处理 SHA-256 为
  `c0f31c39327b2d7e46d12dcefc9908ca2e0f46bba4331e3f90fd22dcc3834157`
  的 `ap01-1.0.2_0031-opt-rewrite-candidate.bin`。
- 继续由 AP01 原厂在线更新程序决定写入位置。
- 不修改参考项目程序、画面服务、网络配置或其他设备。

## 执行顺序

1. 二维码授权并只读查询唯一目标设备。
2. 重新核对官方版本、设备状态、固件文件和构建清单。
3. 上传冻结成品，完整下载回本机并逐字节比较。
4. 使用同一短时地址和同一冻结成品执行仅下载校验。
5. 展示即时安装确认材料，等待用户明确确认。
6. 下发一次安装，持续检查设备状态、重启、上线和更新错误。
7. 自动检查通过后，由用户执行全部页面与物理旋钮验收。

## 验收标准

- 上传对象与冻结成品逐字节一致。
- 设备仅下载校验完成且没有更新错误。
- 正式安装后设备完成重启并重新上线，运行时间重新计数。
- 米家通信、原厂基础页面和官方更新能力保持正常。
- 用户完成完整页面与物理旋钮验收；自动检查不得替代物理验收。

## 安全停止条件

- 官方版本、冻结成品、构建清单或目标设备任一不一致。
- 设备不唯一、离线、更新状态非空闲，或供电、网络不稳定。
- 上传对象与冻结成品无法证明逐字节一致。
- 仅下载失败或出现更新错误。
- 仅下载后发生预期外重启，且当前运行成品身份无法确认。
- 未取得正式安装前的即时确认。
- 下载、安装、重启或重新上线出现异常；不重复盲目下发。
- 凭据、设备标识、固件文件或临时下载地址进入待提交内容。
